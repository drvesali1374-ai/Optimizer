// strategies.js
// این فایل صرفاً برای سازگاری نگه داشته شده، اما دیگر استراتژی ثابتی نداریم.
// در عوض ساختار طرح‌ها (Plan) در app.js مدیریت می‌شود.
window.ALL_STRATEGIES = []; // خالی – اپ جدید از این استفاده نمی‌کند